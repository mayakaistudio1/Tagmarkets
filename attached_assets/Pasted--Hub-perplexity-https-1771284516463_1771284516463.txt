Логично, что этого не хватает. Добавим их так, чтобы не усложнить, а усилить Hub. [perplexity](https://www.perplexity.ai/search/5d658fec-a0a4-48b4-bd2e-51fd4c6f105a)

## Где появляется расписание (Schedule)

### 1. На главном экране (Hub)

В блоке «инфо / сервис» добавь отдельную кнопку:

- `📅 Webinare & Termine`  
  Саб: `Nächste Live-Calls, Events & Schulungen.`  

По клику → отдельный простой экран `Schedule`:

- Список ближайших 3–5 событий:
  - Дата, время, название, тип:  
    `Mi, 20:00 – Trading & Amplify Q&A (Zoom)`  
    `Fr, 19:00 – Partner-Call: Einkommen & Plan`  
  - Кнопки: `Zum Zoom`, `In Kalender speichern`.  
- Фильтры/табы (минимум): `Alle | Trading | Partner`.  

Так ты закрываешь «Расписание» для обеих аудиторий в одном месте.

### 2. Ссылки из Trading Hub и Partner Hub

- Внизу **Trading Hub**: маленькая кнопка  
  `📅 Nächster Trading-Call` → открывает тот же экран `Schedule`, но с фильтром `Trading`.  
- Внизу **Partner Hub**:  
  `📅 Nächster Partner-Call` → тот же экран с фильтром `Partner`.

***

## Где появляется Tutorials

### 1. Отдельная кнопка на главном экране

Добавь ещё одну ссылку рядом с Webinare:

- `🎓 Tutorials & Guides`  
  Саб: `Kurzvideos & Schritt-für-Schritt Anleitungen.`  

По клику → экран `Tutorials`:

- Блок `Für Trader`:
  - `Video: Erste Schritte mit TAG Markets (5 min)`  
  - `Video: Copy-X Strategie auswählen (7 min)`  
  - `Guide: Amplify verstehen (PDF / Video)`.
- Блок `Für Partner`:
  - `Video: Partnerprogramm erklärt (6 min)`  
  - `Guide: Deinen Link richtig teilen` и т.п.

Всего 4–6 туториалов, каждый = карточка: название, 1 строка, кнопка `Ansehen`.

### 2. Точечные входы из других экранов

- В **Trading Hub** под карточками:
  - `🎓 Tutorials zu Trading & Copy-X` → ведёт на тот же `Tutorials` с фильтром `Für Trader`.  
- В **Partner Hub**:
  - `🎓 Partner-Tutorials` → тот же экран с фильтром `Für Partner`.  

***

## В итоге

- Главный экран остаётся простым:  
  Мария → 2 больших пути (Trading / Partner) → блок инфо‑кнопок, где теперь есть:
  - `📅 Webinare & Termine`
  - `🎓 Tutorials & Guides`
- Оба раздела имеют **свой отдельный экран**, но на них ведут ссылки и с Hub, и из внутренних хабов (Trading/Partner), так что пользователь всегда может быстро найти расписание и обучалки, не теряясь. [perplexity](https://www.perplexity.ai/search/644afef4-8f39-4988-9677-f7075f45249b)